shadow$provide.module$node_modules$$mui$x_date_pickers$node$internals$utils$warning = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.buildWarning = exports.buildDeprecatedPropsWarning = void 0;
  exports.buildDeprecatedPropsWarning = message => {
    let alreadyWarned = !1;
    const cleanMessage = Array.isArray(message) ? message.join("\n") : message;
    return deprecatedProps => {
      deprecatedProps = Object.entries(deprecatedProps).filter(([, value]) => void 0 !== value).map(([key]) => `- ${key}`);
      !alreadyWarned && 0 < deprecatedProps.length && (alreadyWarned = !0, console.warn([cleanMessage, "deprecated props observed:", ...deprecatedProps].join("\n")));
    };
  };
  exports.buildWarning = (message, gravity = "warning") => {
    let alreadyWarned = !1;
    const cleanMessage = Array.isArray(message) ? message.join("\n") : message;
    return () => {
      alreadyWarned || (alreadyWarned = !0, "error" === gravity ? console.error(cleanMessage) : console.warn(cleanMessage));
    };
  };
};

//# sourceMappingURL=module$node_modules$$mui$x_date_pickers$node$internals$utils$warning.js.map
