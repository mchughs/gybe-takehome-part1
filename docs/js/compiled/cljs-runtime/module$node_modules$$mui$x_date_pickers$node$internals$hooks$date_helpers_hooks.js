shadow$provide.module$node_modules$$mui$x_date_pickers$node$internals$hooks$date_helpers_hooks = function(global, require, module, exports) {
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.useMeridiemMode = function(date, ampm, onChange, selectionState) {
    const utils = (0,_useUtils.useUtils)(), meridiemMode = (0,_timeUtils.getMeridiem)(date, utils), handleMeridiemChange = React.useCallback(mode => {
      mode = null == date ? null : (0,_timeUtils.convertToMeridiem)(date, mode, !!ampm, utils);
      onChange(mode, selectionState ?? "partial");
    }, [ampm, date, onChange, selectionState, utils]);
    return {meridiemMode, handleMeridiemChange};
  };
  exports.useNextMonthDisabled = function(month, {disableFuture, maxDate, timezone}) {
    const utils = (0,_useUtils.useUtils)();
    return React.useMemo(() => {
      var now = utils.date(void 0, timezone);
      now = utils.startOfMonth(disableFuture && utils.isBefore(now, maxDate) ? now : maxDate);
      return !utils.isAfter(now, month);
    }, [disableFuture, maxDate, month, utils, timezone]);
  };
  exports.usePreviousMonthDisabled = function(month, {disablePast, minDate, timezone}) {
    const utils = (0,_useUtils.useUtils)();
    return React.useMemo(() => {
      var now = utils.date(void 0, timezone);
      now = utils.startOfMonth(disablePast && utils.isAfter(now, minDate) ? now : minDate);
      return !utils.isBefore(now, month);
    }, [disablePast, minDate, month, utils, timezone]);
  };
  var React = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != typeof e && "function" != typeof e) {
      return {default:e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && Object.prototype.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n.default = e, r && r.set(e, n), n;
  }(require("module$node_modules$react$index")), _useUtils = require("module$node_modules$$mui$x_date_pickers$node$internals$hooks$useUtils"), _timeUtils = require("module$node_modules$$mui$x_date_pickers$node$internals$utils$time_utils");
};

//# sourceMappingURL=module$node_modules$$mui$x_date_pickers$node$internals$hooks$date_helpers_hooks.js.map
